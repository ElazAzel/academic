{
  "seq": 9,
  "name": "Fix XLSX Row Counter Bug",
  "group": "F",
  "module": "reports",
  "description": "In buildProgressSheet(), after ws.mergeCells(...), rowNum variable is NOT incremented. This causes subsequent data rows to overwrite the course header. Fix: increment rowNum after each mergeCells call and after each summary row. Verify all row references are correct.",
  "files": [
    "lib/reports/xlsx-generator.ts"
  ],
  "context_files": [
    "AI.md",
    "ai/roles/backend-next-prisma.md"
  ],
  "reference_files": [],
  "depends_on": [],
  "parallel": true
}
