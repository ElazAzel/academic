{
  "seq": 11,
  "name": "Fix ChatPanel Optimistic Indicator",
  "group": "H",
  "module": "chat",
  "description": "The 'отправляется...' indicator (m.id.startsWith('optimistic-')) stays visible even if the message fails. Fix: use sendMutation.isError status to detect failed messages. Add error state styling for failed messages (red border, error icon). Add retry button for failed messages. Remove optimistic messages from UI when they fail (after showing error briefly).",
  "files": [
    "components/lms/chat-panel.tsx"
  ],
  "context_files": [
    "AI.md",
    "ai/roles/frontend-lms-ux.md"
  ],
  "reference_files": [
    "server/actions/chat.ts",
    "components/lms/ask-curator-question.tsx"
  ],
  "depends_on": [],
  "parallel": true
}
