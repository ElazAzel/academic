name: CI

on:
  push:
    branches: [main, "audit/*", "fix/*"]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: "20"
  DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/academy_test?schema=public"

jobs:
  lint-typecheck-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: academy_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - run: npm ci

      - run: npx prisma generate

      - run: npx prisma db push
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - run: npx tsc --noEmit
        name: TypeScript check

      - run: npm run lint -- --max-warnings=0
        name: ESLint

      - run: npm run test
        name: Unit & integration tests

      - run: npm run build
        name: Build
